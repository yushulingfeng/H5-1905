const baseUrl = ' ../src/database'
const requestUrl = {
    list: baseUrl + "/list.json",
    user: baseUrl + "/user.json",
    goods: baseUrl + "/goods.json",
    navList: baseUrl + "/navList.json",
    flashSale: baseUrl + "/flashSale.json"
}

export {
    requestUrl
}